# Known Good — 2026-08-17

## Tree drag handle restored

**Status: KNOWN GOOD / WORKING**

Verified on `dev` after manual Cloudflare rebuild and hard refresh.

Verified:
- Tree `⠿` drag handle is visible again.
- Drag handle works for tree reordering/reparenting.
- Tree expand/collapse remains working.
- Fix deployed from commit `ec5733636d92861225d3e6825d19e908d22dd8b1`.

If the tree drag handle regresses, compare against this checkpoint before patching forward.
